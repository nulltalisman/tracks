source "https://rubygems.org"

group :jekyll_plugins do
  gem "github-pages", "~> 228", require: false
  gem "jekyll-paginate-v2"  # add this line
end

group :development do
  gem "webrick"  # needed for Ruby 3.x to serve locally
  gem "wdm", ">= 0.1.0", platforms: [:mingw, :mswin, :x64_mingw]  # improve file watching on Windows
end

# Optional for local overrides (no GitHub Pages)
# gem "jekyll", "~> 3.9"
